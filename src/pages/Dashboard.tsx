import styled from 'styled-components';

const Wrapper = styled.div`
  font-family: 'Montserrat', sans-serif;
`;

const Dashboard = () => {
   return (<Wrapper>Hey I am a Dashboard</Wrapper>);
}

export default Dashboard;
